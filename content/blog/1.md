---
title: "Getting Started with Next.js 13"
date: "2025-04-12"
excerpt: "Learn how to build modern web applications with Next.js 13 and its new app directory features."
coverImage:
  src: "/blog/nextjs-cover.jpg"
  alt: "Next.js application dashboard example"
  caption: "A modern Next.js application interface"
---

# Test Blog Post

Just some random stuff to see if I can make a blog.

![Some image from the web](/blog/app-router.jpg)
*OOOh shiny*

## The next section
This next section follow on from the first one, snazzy

```tsx
// app/page.tsx
export default function HomePage() {
  return (
    <main className="p-8">
      <h1 className="text-4xl font-bold">
        OOoh a code snippet
      </h1>
      <p className="mt-4">
        This is a server component by default!
      </p>
    </main>
  );
}
```

## Another section!
This was all written in markdown by the way, pretty cool.

```tsx
// app/posts/page.tsx
async function getPosts() {
  const res = await fetch('https://api.example.com/posts');
  return res.json();
}

export default async function PostsPage() {
  const posts = await getPosts();
  
  return (
    <ul>
      {posts.map(post => (
        <li key={post.id}>{post.title}</li>
      ))}
    </ul>
  );
}
```

## Data Fetching
The new data fetching system is more intuitive and powerful, allowing for better caching and revalidation strategies.